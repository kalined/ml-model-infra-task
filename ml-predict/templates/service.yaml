apiVersion: v1
kind: Service
metadata:
  name: {{ include "ml-predict.fullname" . }}
  labels:
    {{- include "ml-predict.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ml-predict.selectorLabels" . | nindent 4 }}
